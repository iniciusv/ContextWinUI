<UserControl
    x:Class="ContextWinUI.Views.AiDiffViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ContextWinUI.Models"
    xmlns:converters="using:ContextWinUI.Converters"
    mc:Ignorable="d">

	<UserControl.Resources>
		<converters:BoolToVisibilityConverter x:Key="BoolToVisibleConverter" />
	</UserControl.Resources>

	<Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="0" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" 
              BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="12,8">
			<TextBlock Text="{x:Bind ViewModel.SelectedChange.FilePath, Mode=OneWay}" FontWeight="SemiBold"/>
		</Grid>

		<Grid Grid.Row="1" Background="#FFF8E1" Padding="8">
			<TextBlock Text="{x:Bind ViewModel.DebugInfo, Mode=OneWay}" Foreground="Black" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
		</Grid>

		<ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
			<ItemsControl ItemsSource="{x:Bind ViewModel.GraphLines, Mode=OneWay}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Spacing="0"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate x:DataType="models:GraphCodeLine">
						<Grid Background="{x:Bind BackgroundColor}" Height="20">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="40"/>
								<ColumnDefinition Width="4"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>

							<TextBlock Text="{x:Bind LineNumber}" FontFamily="Consolas" FontSize="11" Foreground="Gray" 
                                       HorizontalAlignment="Right" Margin="0,0,8,0" VerticalAlignment="Center"/>

							<Rectangle Grid.Column="1" Fill="{x:Bind BorderColor}"/>

							<TextBlock Grid.Column="2" 
           Text="{x:Bind Text}" 
           FontFamily="Consolas" 
           FontSize="13" 
           VerticalAlignment="Center" 
           Margin="6,0,0,0" 
           Foreground="{ThemeResource TextFillColorPrimaryBrush}"
           MinWidth="20"/>

							<TextBlock Grid.Column="3" Text="{x:Bind SymbolName}" FontSize="10" Foreground="Gray" 
                                       Opacity="0.6" Margin="0,0,8,0" VerticalAlignment="Center"/>
						</Grid>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>

		<ProgressRing Grid.Row="2" IsActive="True" Width="50" Height="50"
                      Visibility="{x:Bind ViewModel.IsProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibleConverter}}"/>
	</Grid>
</UserControl>