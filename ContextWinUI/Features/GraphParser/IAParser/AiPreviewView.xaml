<UserControl
    x:Class="ContextWinUI.Features.GraphParser.AiPreviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ContextWinUI.Features.GraphParser.ViewModels"
    xmlns:iap="using:ContextWinUI.Features.GraphParser.IAParser"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d">

	<UserControl.Resources>
		<converters:BoolToVisibilityConverter x:Key="BoolToVis" />
	</UserControl.Resources>

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Border Grid.Row="0" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Padding="16">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<StackPanel Spacing="4">
					<TextBlock Text="DEBUG DA ANÁLISE IA" 
                               FontWeight="Bold" 
                               Foreground="{ThemeResource SystemAccentColor}"
                               IsTextSelectionEnabled="True"/>

					<StackPanel Orientation="Horizontal" Spacing="8">
						<TextBlock Text="Arquivo Alvo:" FontWeight="SemiBold" IsTextSelectionEnabled="True"/>
						<TextBlock Text="{x:Bind ViewModel.Result.FilePath}" Opacity="0.8" IsTextSelectionEnabled="True"/>
					</StackPanel>

					<StackPanel Orientation="Horizontal" Spacing="16">
						<TextBlock Text="{x:Bind ViewModel.Result.ModifiedCount}" Foreground="Orange" IsTextSelectionEnabled="True"/>
						<TextBlock Text=" Modificados" Foreground="Orange" IsTextSelectionEnabled="True"/>
						<TextBlock Text=" | " IsTextSelectionEnabled="True"/>
						<TextBlock Text="{x:Bind ViewModel.Result.AddedCount}" Foreground="Green" IsTextSelectionEnabled="True"/>
						<TextBlock Text=" Adicionados" Foreground="Green" IsTextSelectionEnabled="True"/>
					</StackPanel>

					<TextBlock Text="{x:Bind ViewModel.Result.Message}" 
                               FontStyle="Italic" Margin="0,4,0,0" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               IsTextSelectionEnabled="True"/>
				</StackPanel>

				<StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
					<Button Content="Descartar" Command="{x:Bind ViewModel.CancelCommand}"/>
					<Button Content="Aplicar Alterações" Style="{StaticResource AccentButtonStyle}" 
                            Command="{x:Bind ViewModel.ConfirmAndApplyCommand}"/>
				</StackPanel>
			</Grid>
		</Border>

		<ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.PreviewRows}" SelectionMode="None">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="vm:SegmentRowViewModel">
					<Grid Padding="12,4" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="50"/>
							<ColumnDefinition Width="200"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<FontIcon Glyph="&#xE7ba;" 
                                  Visibility="{x:Bind Current.HasUnsavedChanges, Converter={StaticResource BoolToVis}}"
                                  Foreground="Orange" HorizontalAlignment="Left" FontSize="12"/>

						<TextBlock Grid.Column="1" Text="{x:Bind Current.Name}" VerticalAlignment="Center" FontWeight="SemiBold"/>

						<TextBlock Grid.Column="2" Text="{x:Bind Current.Content}" 
                                   FontFamily="Consolas" FontSize="12" 
                                   MaxLines="1" TextTrimming="CharacterEllipsis" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   IsTextSelectionEnabled="True"/>
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</UserControl>