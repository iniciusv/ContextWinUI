<UserControl
    x:Class="ContextWinUI.Views.FileContentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent">
			<AppBarButton x:Name="BtnEdit" Icon="Edit" Label="Editar" Click="BtnEdit_Click" />
			<AppBarButton x:Name="BtnSave" Icon="Save" Label="Salvar" Command="{x:Bind ContentViewModel.SaveContentCommand, Mode=OneWay}" Visibility="Collapsed" />
			<AppBarButton x:Name="BtnCancel" Icon="Cancel" Label="Cancelar" Click="BtnCancel_Click" Visibility="Collapsed"/>
			<AppBarSeparator/>

			<AppBarButton Icon="Copy" Label="Copiar" Command="{x:Bind CopySelectedCommand, Mode=OneWay}" />
			<AppBarButton Icon="Refresh" Label="Analisar" Command="{x:Bind AnalyzeCommand, Mode=OneWay}" />
		</CommandBar>

		<ScrollViewer x:Name="MainScrollViewer"
                  Grid.Row="1"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  ZoomMode="Enabled"
                  MinZoomFactor="0.5"
                  MaxZoomFactor="4.0"
                  PointerWheelChanged="MainScrollViewer_PointerWheelChanged">

			<Grid Margin="0,0,20,20">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" MinWidth="40"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>

				<TextBlock x:Name="LineNumbersDisplay"
                       Grid.Column="0"
                       FontFamily="Consolas, Courier New, Monospace"
                       FontSize="14"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       TextAlignment="Right"
                       Margin="0,12,10,0"
                       IsHitTestVisible="False"/>

				<RichTextBlock x:Name="CodeViewer"
                           Grid.Column="1"
                           FontFamily="Consolas, Courier New, Monospace"
                           FontSize="14"
                           IsTextSelectionEnabled="True"
                           Padding="0,12,12,12"/>

				<RichEditBox x:Name="CodeEditor"
                         Grid.Column="1"
                         Visibility="Collapsed"
                         IsSpellCheckEnabled="False"
                         IsTextPredictionEnabled="False"
                         FontFamily="Consolas, Courier New, Monospace"
                         FontSize="14"
                         TextChanged="CodeEditor_TextChanged"
                         KeyDown="CodeEditor_KeyDown"
                         AcceptsReturn="True"
                         BorderThickness="0"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         TextWrapping="NoWrap" />
			</Grid>
		</ScrollViewer>

	</Grid>
</UserControl>