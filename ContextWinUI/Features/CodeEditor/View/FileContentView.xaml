<UserControl
    x:Class="ContextWinUI.Views.FileContentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:ContextWinUI.Views.Components"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent">
			<AppBarButton x:Name="BtnEdit" Icon="Edit" Label="Editar" Click="BtnEdit_Click" />
			<AppBarButton x:Name="BtnSave" Icon="Save" Label="Salvar" Command="{x:Bind ContentViewModel.SaveContentCommand, Mode=OneWay}" Visibility="Collapsed" />
			<AppBarButton x:Name="BtnCancel" Icon="Cancel" Label="Cancelar" Click="BtnCancel_Click" Visibility="Collapsed"/>
			<AppBarSeparator/>
			<AppBarButton Icon="Setting" Label="Opções de Visualização">
				<AppBarButton.Flyout>
					<Flyout>
						<StackPanel Spacing="10" Width="250">
							<TextBlock Text="Filtros de Visualização" Style="{StaticResource SubtitleTextBlockStyle}"/>
							<ToggleSwitch x:Name="ToggleComments" 
                                          Header="Ocultar Comentários" 
                                          Toggled="OnViewOptionChanged" />
							<ToggleSwitch x:Name="ToggleCollapse" 
                                          Header="Simplificar Métodos Longos" 
                                          Toggled="OnViewOptionChanged" />
							<StackPanel Visibility="{x:Bind ToggleCollapse.IsOn, Mode=OneWay}">
								<TextBlock Text="Ocultar se maior que (linhas):" Style="{StaticResource CaptionTextBlockStyle}"/>
								<NumberBox x:Name="NumCollapseLines" 
                                           Value="15" 
                                           Minimum="5" 
                                           Maximum="200" 
                                           SpinButtonPlacementMode="Inline"
                                           ValueChanged="OnViewOptionChanged_Number" />
							</StackPanel>
						</StackPanel>
					</Flyout>
				</AppBarButton.Flyout>
			</AppBarButton>
			<AppBarSeparator/>
			<AppBarButton Icon="Copy" Label="Copiar" Command="{x:Bind CopySelectedCommand, Mode=OneWay}" />
			<AppBarButton Icon="Refresh" Label="Analisar" Command="{x:Bind AnalyzeCommand, Mode=OneWay}" />
		</CommandBar>

		<Grid Grid.Row="1">
			<components:CodeViewerControl x:Name="ViewerControl"
                                          Visibility="Visible"
                                          Text="{x:Bind ContentViewModel.FileContent, Mode=OneWay}"
                                          FileExtension="{x:Bind ContentViewModel.SelectedItem.SharedState.Extension, Mode=OneWay, FallbackValue='.txt'}" />

			<components:CodeEditorControl x:Name="EditorControl"
                                          Visibility="Collapsed"
                                          Text="{x:Bind ContentViewModel.FileContent, Mode=TwoWay}"
                                          FileExtension="{x:Bind ContentViewModel.SelectedItem.SharedState.Extension, Mode=OneWay, FallbackValue='.txt'}"
                                          SaveRequested="OnEditorSaveRequested" />

			<Grid Background="{ThemeResource LayerFillColorDefaultBrush}" 
                  Visibility="{x:Bind ContentViewModel.IsLoading, Mode=OneWay}"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
				<ProgressRing IsActive="True" Width="40" Height="40"/>
			</Grid>
		</Grid>
	</Grid>
</UserControl>