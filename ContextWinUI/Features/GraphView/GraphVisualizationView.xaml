<UserControl
    x:Class="ContextWinUI.Features.GraphView.GraphVisualizationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:ContextWinUI.Features.GraphView"
    xmlns:viewmodels="using:ContextWinUI.ViewModels"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:GraphLegendControl 
            Grid.Row="0" 
            ViewModel="{x:Bind ViewModel, Mode=OneWay}"/>

        <Pivot Grid.Row="1">

            <PivotItem Header="Arquivo Selecionado">
                <local:CodeViewerControl 
                    Text="{x:Bind ViewModel.FileContent, Mode=OneWay}"
                    FileExtension="{x:Bind ViewModel.CurrentExtension, Mode=OneWay}"
                    HighlighterStrategy="{x:Bind ViewModel.Strategy, Mode=OneWay}" />
            </PivotItem>

            <PivotItem Header="Análise de Snippet">
                <local:SnippetAnalysisView 
                    ViewModel="{x:Bind ViewModel, Mode=OneWay}"/>
            </PivotItem>

        </Pivot>
    </Grid>
</UserControl>