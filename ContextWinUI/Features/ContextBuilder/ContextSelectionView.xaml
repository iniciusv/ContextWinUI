<UserControl
    x:Class="ContextWinUI.Features.ContextBuilder.ContextSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:ContextWinUI.Models"
    xmlns:components="using:ContextWinUI.Views.Components"
    xmlns:converters="using:ContextWinUI.Converters"
    x:Name="RootSelectionView">

	<UserControl.Resources>
		<converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
	</UserControl.Resources>

	<Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent" IsOpen="True">
			<AppBarButton Icon="Save" Label="Salvar" Command="{x:Bind ViewModel.SaveSelectionListCommand}"/>
			<AppBarButton Icon="OpenFile" Label="Carregar" Command="{x:Bind ViewModel.LoadSelectionListCommand}"/>
			<AppBarButton Icon="Paste" Label="Importar" Command="{x:Bind ViewModel.ImportFromTextCommand}" CommandParameter="{x:Bind XamlRoot}"/>
			<AppBarButton Icon="Clear" Label="Limpar" Command="{x:Bind ViewModel.ClearCommand}"/>
		</CommandBar>

		<TextBlock Grid.Row="1" Text="Itens marcados:" 
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                   FontSize="12" Margin="16,4,16,8"/>

		<ListView Grid.Row="2" 
                  ItemsSource="{x:Bind ViewModel.DisplayedItems, Mode=OneWay}" 
                  SelectionMode="Extended"
                  IsItemClickEnabled="True"
                  ItemClick="OnListViewItemClick"
                  Padding="0,0,0,12">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="HorizontalContentAlignment" Value="Stretch" />
					<Setter Property="Padding" Value="0"/>
					<Setter Property="MinHeight" Value="32"/>
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="models:FileSystemItem">
					<Grid Margin="{x:Bind SelectionMargin, Mode=OneWay}" 
                          Background="{x:Bind SelectionBackground, Mode=OneWay}"
                          Padding="12,4" 
                          CornerRadius="4">
						<Grid.ContextFlyout>
							<Flyout Opening="OnTagMenuOpening" />
						</Grid.ContextFlyout>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>
						<CheckBox Grid.Column="0" 
                                  IsChecked="{x:Bind IsChecked, Mode=TwoWay}" 
                                  MinWidth="0" Margin="0,0,12,0" 
                                  VerticalAlignment="Center"/>
						<StackPanel Grid.Column="1" VerticalAlignment="Center">
							<StackPanel Orientation="Horizontal" Spacing="8">
								<FontIcon Glyph="{x:Bind Icon}" FontSize="12" 
                                          Foreground="{ThemeResource SystemAccentColor}"/>
								<TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="13"/>
								<components:TagChipsControl TagsSource="{x:Bind SharedState.Tags, Mode=OneWay}"/>
							</StackPanel>
							<TextBlock Text="{x:Bind FullPath}" 
                                       FontSize="10" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                       TextTrimming="CharacterEllipsis"/>
						</StackPanel>
						<TextBlock Grid.Column="2" Text="{x:Bind Type}" 
                                   FontSize="10" Margin="8,0,0,0" 
                                   VerticalAlignment="Center" Opacity="0.5"/>
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8"
                    Visibility="{x:Bind ViewModel.SelectedItemsList.Count, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter='Invert', Mode=OneWay}">
			<FontIcon Glyph="&#xE916;" FontSize="32" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
			<TextBlock Text="Nenhum item no contexto" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
		</StackPanel>

		<Grid Grid.Row="3" Padding="16,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
			<CheckBox Content="Ocultar subitens (métodos, classes)" 
                      IsChecked="{x:Bind ViewModel.HideSubItems, Mode=TwoWay}"
                      ToolTipService.ToolTip="Exibe apenas os arquivos na lista, mas mantém os subitens selecionados para a cópia."/>
		</Grid>
	</Grid>
</UserControl>