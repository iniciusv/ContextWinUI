<UserControl
    x:Class="ContextWinUI.Features.DatabaseContext.Views.DatabaseSelectionView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ContextWinUI.Features.DatabaseContext.ViewModels"
    xmlns:models="using:ContextWinUI.Models"
    xmlns:helpers="using:ContextWinUI.Helpers"
    mc:Ignorable="d">

    <UserControl.Resources>
        <helpers:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Padding="8,8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Database Context" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12">
                    <Run Text="Selected Items:"/>
                    <Run Text="{x:Bind ViewModel.SelectedTablesCount, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
            
            <!-- Optional Top Actions -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                <Button Command="{x:Bind ViewModel.ClearSelectionCommand}" 
                        ToolTipService.ToolTip="Clear All"
                        Background="Transparent" BorderThickness="0" Padding="6">
                    <FontIcon Glyph="&#xE74D;" FontSize="14"/> <!-- Delete Icon -->
                </Button>
            </StackPanel>
        </Grid>

        <!-- Sub-header / Info -->
        <TextBlock Grid.Row="1" Text="Selected tables:" 
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                   FontSize="12" Margin="16,12,16,4"/>

        <!-- List -->
        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.SelectedForContext, Mode=OneWay}"
                  SelectionMode="None"
                  Padding="0,0,0,12">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="MinHeight" Value="32"/>
                </Style>
            </ListView.ItemContainerStyle>
            
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:DatabaseSchemaDto">
                    <Grid Padding="12,4" CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon -->
                        <FontIcon Grid.Column="0" Glyph="&#xE7F8;" FontSize="14" 
                                  Foreground="{ThemeResource SystemAccentColor}"
                                  Margin="0,0,12,0" VerticalAlignment="Center"/>

                        <!-- Text -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind TableName}" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="{x:Bind Schema}" 
                                       FontSize="10" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <!-- Remove Action -->
                        <Button Grid.Column="2" 
                                Command="{Binding ElementName=Root, Path=ViewModel.RemoveTableCommand}" 
                                CommandParameter="{x:Bind}"
                                Background="Transparent" BorderThickness="0"
                                Padding="4" Margin="8,0,0,0"
                                ToolTipService.ToolTip="Remove from context">
                            <FontIcon Glyph="&#xE711;" FontSize="10"/>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8"
                    Visibility="{x:Bind ViewModel.SelectedTablesCount, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter='Invert', Mode=OneWay}">
            <FontIcon Glyph="&#xE7F8;" FontSize="32" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
            <TextBlock Text="No tables selected" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
        </StackPanel>

        <!-- Bottom Action (Copy) -->
        <Grid Grid.Row="3" 
              Padding="16" 
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
              BorderThickness="0,1,0,0">
            <Button HorizontalAlignment="Stretch" 
                    Style="{StaticResource AccentButtonStyle}" 
                    Command="{x:Bind ViewModel.CopyToClipboardCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE8C8;"/>
                    <TextBlock Text="Copy to Clipboard"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
