<UserControl
    x:Class="ContextWinUI.Features.DatabaseContext.Views.DatabaseSchemaDataView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ContextWinUI.Features.DatabaseContext.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status / Header -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Padding="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoadingData, Mode=OneWay}" Width="16" Height="16" Margin="0,0,8,0" Visibility="{x:Bind ViewModel.IsLoadingData, Mode=OneWay}"/>
            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Schema (Top) -->
        <ListView Grid.Row="0" ItemsSource="{x:Bind ViewModel.Columns, Mode=OneWay}" SelectionMode="None">
            <ListView.Header>
                <Grid Padding="12,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Column" FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Text="Type" FontWeight="Bold"/>
                    <TextBlock Grid.Column="2" Text="Nullable" FontWeight="Bold"/>
                    <TextBlock Grid.Column="3" Text="PK" FontWeight="Bold"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Grid.Column="1" Text="{Binding DataType}"/>
                        <CheckBox Grid.Column="2" IsChecked="{Binding IsNullable, Mode=OneWay}" IsEnabled="False"/>
                        <CheckBox Grid.Column="3" IsChecked="{Binding IsPrimaryKey, Mode=OneWay}" IsEnabled="False"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <controls:GridSplitter Grid.Row="1" ResizeBehavior="BasedOnAlignment" ResizeDirection="Rows"/>

        <!-- Data Sample (Bottom) -->
        <ScrollViewer Grid.Row="2">
             <ItemsControl ItemsSource="{x:Bind ViewModel.DataSamples, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="4" TextWrapping="Wrap"/> 
                        <!-- Simplification: Just showing string representation as generic Dictionary binding is hard in XAML without converters or dynamic generation -->
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
             </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
