<UserControl
    x:Class="ContextWinUI.Views.FileContentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent">
			<AppBarButton Icon="Save" Label="Salvar" Command="{x:Bind ContentViewModel.SaveContentCommand, Mode=OneWay}" ToolTipService.ToolTip="Salvar alterações (Ctrl+S)"/>
			<AppBarSeparator/>
			<AppBarButton Icon="Copy" Label="Copiar Arquivo" Command="{x:Bind CopySelectedCommand, Mode=OneWay}" ToolTipService.ToolTip="Copia todo o conteúdo do arquivo" />
			<AppBarButton Icon="Refresh" Label="Analisar" Command="{x:Bind AnalyzeCommand, Mode=OneWay}" />
		</CommandBar>

		<RichEditBox x:Name="CodeEditor"
                     Grid.Row="1"
                     IsSpellCheckEnabled="False"
                     IsTextPredictionEnabled="False"
                     FontFamily="Consolas, Courier New, Monospace"
                     FontSize="14"
                     TextChanged="CodeEditor_TextChanged"
                     KeyDown="CodeEditor_KeyDown"
                     AcceptsReturn="True"
                     BorderThickness="0"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap" />

		<Grid Grid.Row="1" Background="{ThemeResource LayerFillColorDefaultBrush}" 
              Visibility="{x:Bind ContentViewModel.IsLoading, Mode=OneWay}"
              HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<ProgressRing IsActive="True" Width="40" Height="40"/>
		</Grid>
	</Grid>
</UserControl>