 <UserControl
    x:Class="ContextWinUI.Views.FileContentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Grid Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<TextBlock Text="{x:Bind ContentViewModel.SelectedItem.Name, Mode=OneWay, FallbackValue='Nenhum arquivo selecionado'}"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center"/>

			<Button Grid.Column="1" Content="Analisar Contexto" Command="{x:Bind AnalyzeCommand}" Style="{StaticResource AccentButtonStyle}" Margin="0,0,8,0"/>

			<Button Grid.Column="2" Content="Copiar Arquivo" Command="{x:Bind ContentViewModel.CopyToClipboardCommand}" Margin="0,0,8,0">
				<Button.KeyboardAccelerators>
					<KeyboardAccelerator Key="C" Modifiers="Control"/>
				</Button.KeyboardAccelerators>
			</Button>

			<Button Grid.Column="3" Content="Copiar Selecionados" Command="{x:Bind CopySelectedCommand}" Style="{StaticResource AccentButtonStyle}">
				<Button.KeyboardAccelerators>
					<KeyboardAccelerator Key="C" Modifiers="Control,Shift"/>
				</Button.KeyboardAccelerators>
			</Button>
		</Grid>

		<Grid Grid.Row="1">
			<ScrollViewer Padding="16"
                          ZoomMode="Enabled"
                          MinZoomFactor="0.5"
                          MaxZoomFactor="3.0"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">

				<RichTextBlock x:Name="CodeRichTextBlock" 
                               FontFamily="Cascadia Code, Consolas, Courier New" 
                               FontSize="13" 
                               IsTextSelectionEnabled="True" 
                               TextWrapping="NoWrap" />
			</ScrollViewer>

			<Grid Background="{ThemeResource LayerFillColorDefaultBrush}" Visibility="{x:Bind ContentViewModel.IsLoading, Mode=OneWay}">
				<ProgressRing IsActive="True" Width="50" Height="50"/>
			</Grid>
		</Grid>
	</Grid>
</UserControl>